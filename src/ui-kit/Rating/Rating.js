import React from 'react';
import classNames from 'classnames';

const Rating = ({ rating, className }) => {
  return <div className={classNames('rating', className)}></div>;
};

export default Rating;
